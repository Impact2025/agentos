# Secrets — nooit committen
.env
google-credentials.json
*.pem
*.key

# Python
.venv/
__pycache__/
*.pyc

# Node
node_modules/

# Databases & runtime-data
data/*.db
data/*.db.bak*
# Per-klant instances (Nicole, en elke volgende) — eigen DB/vault, nooit in git.
data/*/impactos.db
data/*/impactos.db.bak*
.env.*
# Launch-scripts per klant bevatten een plaintext IMPACTOS_PASSWORD (de
# simpelste manier om instance-config te scheiden zonder een tweede .env-
# loader te bouwen — zie shared/config.py). Nooit committen. Het kale
# impactos_service.cmd (hoofdinstallatie, geen wachtwoord erin) blijft wel getrackt.
impactos_service_*.cmd
# Werkmappen van Claude Code-sessies (snapshot, backtest-scripts, voorstel).
# Ze zijn het artefact waar invest_proposals.backtest_ref naar wijst en horen
# dus op schijf te blijven staan — maar niet in git: ~1,3 MB koersdata per run.
data/claude_code/
database.db
impactos.db
*.pid

# Logs & procesuitvoer
logs/
*.log
*.out
*.err
impactos.err
_out.txt

# Door agents gegenereerde werkruimtes (staan in Obsidian/DB als bron van waarheid)
workspaces/
app/uploads/

# Build-artefacten
frontend/tailwind.css

# OS
Thumbs.db
.DS_Store
backend/data/

# Extra secrets & scratch — nooit committen
.env.bak*
_cookies.txt
publish_env_additions.txt

# Wegwerp-scripts van handmatig uitzoekwerk (poll_*, run_*, upgrade_*, …).
# Ze horen niet in de historie: ze hebben eenmalig gedraaid en verwijzen naar
# id's die niet meer bestaan. Weggooien mag, maar ze staan hier zodat een
# half-afgemaakte reparatie niet stilletjes verdwijnt.
_scratch/

# Door agents geproduceerde project-output & goal-werkmappen (media, zwaar)
projects/
scripts/.tbi_import_state.json

# Windows-artefact
nul

.vercel

.vercel
