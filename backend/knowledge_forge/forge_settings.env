# Knowledge Forge — embeddings-backend config (GEEN secrets).
# Aparte Ollama-instance met nomic-embed-text voor dense vectors.
# Deze file bevat geen credentials en wordt geladen door embeddings.py.
KNOWLEDGE_FORGE_EMBED_URL=http://127.0.0.1:11436
KNOWLEDGE_FORGE_EMBED_MODEL=nomic-embed-text
